# Tonight's Task — [fill in date]

## Milestone
[e.g. v0.4.0 Step 1 — SchemaUIConfig model]

## Prompt
[paste the step prompt block from the milestone file here]

## Constraints
- Run `pytest` after every file change; stop if existing tests regress
- Do not create a migration without verifying it applies cleanly: `python manage.py migrate`
- Do not push to remote
- Commit with message: "[milestone] [step description]"
- If blocked for more than 3 attempts on the same issue, write the blocker to
  docs/roadmap/BLOCKED.md and stop cleanly
- Write a summary of all changes to docs/roadmap/MORNING_REVIEW.md before stopping

## Out of Scope Tonight
[list anything you do not want touched]
